# ──────────────────────────────────────────────
# MatchNearby API — Environment Configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ──────────────────────────────────────────────

# API-Football key — sign up at https://www.api-football.com/
# Found in your dashboard after registration.
API_FOOTBALL_KEY=your_api_key_here

# Supabase credentials — find these in Supabase Dashboard → Settings → API
# Use the "service_role" key for server-side access (never expose to frontend).
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your_service_role_key_here

# Leagues to fetch — comma-separated API-Football league IDs.
# Add or remove leagues without changing code.
# PL=39, La Liga=140, Bundesliga=78, Serie A=135, Ligue 1=61, UCL=2, Europa=3
LEAGUE_IDS=39,140,78,135,61,2,3

# Football season year (e.g., 2025 for the 2025-26 season).
SEASON=2025

# How many upcoming fixtures to fetch per league when no date is specified.
FIXTURES_NEXT=50

# How many hours before synced data is considered stale.
# Lower = fresher data but more API calls. Higher = fewer API calls.
# With 7 leagues at 6 hours, worst case is ~28 API calls/day (free tier allows 100).
SYNC_FRESHNESS_HOURS=6

# Server binding — 0.0.0.0 is required for containers (Railway, Docker).
HOST=0.0.0.0
PORT=8000

# CORS — comma-separated frontend URLs allowed to call this API.
# Change to your production frontend URL when deploying.
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
